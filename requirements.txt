# Dependências do GELOSAR — versões do ambiente local (python -c / importlib).
# Apenas o que o projeto usa de fato. Sem Pillow (FileField, não ImageField).
# Sem Gunicorn/Whitenoise no código atual.
Django==5.0.3
asgiref==3.7.2
sqlparse==0.4.4
tzdata==2023.3
# Driver PostgreSQL para Django 5.0 (backend django.db.backends.postgresql).
# Não é carregado no desenvolvimento com SQLite.
psycopg[binary]>=3.1,<3.3
